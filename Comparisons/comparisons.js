  let x = "df";
  x = Number(x);
  console.log('ereff');
  if (inNaN(x)) {
      console.log("input is not a number");
  } else {
      console.log(x < 18 ? "Too young" : "Old enough");
  }

// using js switch case
switch (new Date().getDate()){

}